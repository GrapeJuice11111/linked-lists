class LinkedLists {
  constructor(value) {
    this.Node = Node;
    this.index = index;
  }

  append(key, value) {
    return new Node(key, value);
  }

  prepend(value) {
    this.value = value;
  }

  size() {}

  head() {}

  tail() {}

  atIndex(index) {}

  pop() {}

  contains() {}

  findValue() {}

  toString() {}
}

class Node {
  constructor(key, value) {
    this.key = key;
    this.next = next;
  }
}

const list = new LinkedLists(Nodes);
console.log(Nodes);
